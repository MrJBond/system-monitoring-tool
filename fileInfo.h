#include "fileReader.h"

namespace systemMonitor{
    class InfoReader{
        public:
          void info(); // print info
          ~InfoReader() = default;
           InfoReader() = default;
    };
}
